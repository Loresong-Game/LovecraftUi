# GALLERY — the index of proof

One row per example page: its purpose, the public exports it demonstrates (`@shows`),
and its URL params. Every page carries the same facts in a `@demo/@shows/@params`
header comment; the static checker keeps both sides truthful (this table and
`examples/` list exactly the same pages;each page's header block exists, its
`@shows` names really are imported from the public entry, and — this
table's Shows column mirrors each page's `@shows` exactly, both directions). `?debug=1` works on every
page (the engine-level overlay: bounds, hit chain, clip rects, perf HUD) and is not
repeated per row; the census/walker params (`?audit=1`, `?torture=1`, `?resize=1`)
are likewise engine-armed on every page the test suite drives and are not repeated either
— the Params column mirrors each page's own `@params` header. Gate column: how the page manifest drives the page.

| Page | Purpose | Shows | Params | Gate |
|---|---|---|---|---|
| game-hud.html | The 1.0 flagship: a complete combat HUD, fought in a drowned hall of cyclopean masonry with the tide standing on its floor. | UnitFrame, ActionBar, Globe, CastBar, Minimap, QuestTracker, ChatBox, ProgressBar, FloatText, EldToast, EldDragDrop, RadialMenu, UINode, QuadNode | ?freeze=1, ?assert=1 | READY |
| demo.html | The full interactive showcase: every widget, window, and system on one scrolling page. | EldWindow, EldPopup, EldDragDrop, EldTooltip, EldEvents, Button, IconButton, TextField, Toggle, Radio, Slider, Select, ProgressBar, EventLog, Table, UnitFrame, Minimap, QuestTracker, CharacterPanel, SettingsPanel, Globe, CastBar, RadialMenu | ?freeze=1, ?assert=1, ?scroll=N, ?spawn=1, ?shrink=1, ?spawnchar=1, ?gamepad=1 | READY |
| rich-text.html | The markup sampler: styled runs, inline icons, a live editor, and non-Latin torture strings, laid out on a scriptorium desk. | EldWindow, EventLog, EldToast, RadialMenu, Button, TextField, VBox, TextNode, UINode, QuadNode | ?freeze=1, ?assert=1 | READY |
| juice.html | Choreography and echoes: the transition gallery, a stagger playground, the sound map, and notify routing, set out on a rehearsal stage. | animateIn, animateOut, stagger, EldNotify, EldWindow, RadialMenu, Button, Slider, ToggleLabel, EventLog, VBox, HBox, GridBox, UINode, NineSliceNode, QuadNode, TextNode | ?freeze=1, ?assert=1 | READY |
| binding.html | The developer's bench: one fake model drives the HUD both directions, a DevPanel cheats at it, a drop-down dev console writes the same store by command, the node inspector walks the live tree, and the window layout persists. | Store, bind, bindText, bindEnabled, DevPanel, DebugConsole, DebugInspector, RadialMenu, EldWindow, Button, Slider, ToggleLabel, TextField, ProgressBar, VBox, TextNode | ?freeze=1, ?assert=1 | READY |
| big-data.html | Ten thousand things: a log firehose, a 5k-row table with live sort, and a focusable 10k list — all pooled, on the desk of a harbour tide office. | EldWindow, EventLog, Table, VirtualList, EldToast, RadialMenu, Button, ToggleLabel, TextNode, UINode, QuadNode | ?freeze=1, ?assert=1 | READY |
| meters.html | Meters & Measures: a wall of live instruments on one fake ticker, and the hand-operated ones beside it. | SegmentedBar, ArcGauge, Badge, attachBadge, ResourceChip, ResourceBar, BigTimer, StarRating, SegmentedControl, SpeedControl, RadialMenu, EldWindow, Button, TextNode, UINode | ?freeze=1, ?assert=1 | READY |
| meta-screens.html | Thresholds: the full first-and-last-five-minutes flow — title menu, saves, difficulty, pause, level select, credits — at the top of a drowned harbour stair. | MenuScreen, MenuList, TitleCard, SaveSlotList, LevelSelectGrid, CreditsScroller, RadialMenu, EldWindow, EldPopup, EldToast, Button, TextNode, UINode, QuadNode | ?freeze=1, ?assert=1 | READY |
| tutorial.html | The Guiding Hand: a sequenced tour that walks you through binding a ward at a lamplit bench — reagents that get spent, a strength rail the integrity bar follows, spotlight cutouts, coachmark callouts, four advance modes. | TutorialSequencer, Spotlight, Coachmark, EldWindow, ItemGrid, ProgressBar, EventLog, RadialMenu, Button, Slider, Select, TextNode, UINode, QuadNode | ?freeze=1, ?assert=1 | READY |
| theme-gallery.html | Many Skins: the shipped presets (Verdigris, Brass, Aether) at four couch-distance scales, a Cutting Room whose raw token overrides and seed scrubber layer on top of whichever preset is picked, a live token diff, a paste-ready init line, and photo-mode hiding. | Eldritch, brass, aether, EldWindow, SegmentedControl, Button, TextField, ToggleLabel, Slider, Select, ProgressBar, KeyGlyph, ActionSlot, RadialMenu, TextNode, UINode, QuadNode, COLORS, FONTS | ?theme=brass\|aether, ?scale=1.25\|1.5\|2, ?freeze=1, ?assert=1 | READY |
| strategy-hud.html | The War Table: a drowned coast you can actually command — marquee-select the thralls, right-click to order them across painted ground, spend the tithe to raise more, learn the Forbidden Sciences point by point, and hold the bell when the tide answers. | SelectionMarquee, CommandCard, BuildQueue, TacticalMap, TalentPanes, ResourceBar, SpeedControl, RadialMenu, EldNotify, EldWindow, EldTooltip, EventLog, VBox, TextNode, UINode, QuadNode, COLORS | ?freeze=1, ?assert=1 | READY |
| survival-hud.html | The Long Winter: a camp on a snowfield where the day actually turns — drag to swing the treeline, warmth drains faster after dark, and the chest, the campfire and the recipe browser all read one larder. | RecipeBrowser, ProcessPanel, CompassStrip, DayDial, ItemGrid, ResourceBar, RadialMenu, EldWindow, EldNotify, EldToast, Button, VBox, HBox, TextNode, UINode, QuadNode | ?freeze=1, ?assert=1 | READY |
| mud-hud.html | The Speaking Stones: a MUD in a window — clickable room prose in a pinned scrollback, a command line with history, a map you walk by clicking a room, the body doll taking staged wounds, and ring/amulet fittings that refuse what they cannot wear. | BodyDoll, EquipmentRack, ChatBox, ItemGrid, EldContextMenu, EldNotify, EldDragDrop, ResourceBar, RadialMenu, Button, EldTooltip, UINode | ?freeze=1, ?assert=1 | READY |
| narrative.html | The Whispering Stage: a staged landfall, letterboxed portrait dialogue on a typewriter, a timed choice that commits a real obligation into the tracker, a parchment journal, a found document, one-line subtitles, and a sanity slider that makes the interface itself tremble. | LetterboxBars, TypewriterText, ChoiceList, Journal, DocumentViewer, SubtitleBar, SanityFX, DialogPanel, QuestTracker, LoadingVeil, EldToast, RadialMenu, Slider, TextNode | ?freeze=1, ?assert=1 | READY |
| action-hud.html | The Lantern Bearer: a quay at night with a weapon wheel whose hit area IS the ring (corners and the hole fall through), a scripted boss with phases and an enrage clock, a draining stamina ring, a keycap belt, and five landmarks the anchor layer carries in and out of frame. | RadialMenu, BossBar, ArcGauge, CompassStrip, WorldAnchorLayer, OffscreenIndicator, InteractPrompt, InputPrompt, KeyGlyph, ActionBar, EldDragDrop, TitleCard, TextNode, UINode, QuadNode, NineSliceNode | ?freeze=1, ?assert=1 | READY |
| deckbuilder.html | The Drawn Hand: a real one-fight loop — cards dealt flying from the mound, harm that sweeps enemy bars and shakes plates, ward that soaks the answering blows, intents that telegraph and RESOLVE, deaths that fade the table, a reward pick, and the run map climbing floor by floor. | Card, HandFan, Pile, RunMap, CARD_SIZES, WorldAnchorLayer, ResourceChip, SegmentedBar, ProgressBar, FloatText, TitleCard, RadialMenu, Button, TextNode, UINode, QuadNode, NineSliceNode | ?freeze=1, ?assert=1 | READY |
| shooter-hud.html | The Iron Sights over a LIVING firefight: the mouse pans your aim across a harbor night of drifting horrors, the compass tape slides with your yaw, shots raycast from the fixed reticle — markers stamp ON the enemy, kills feed the board and the score — return fire lights the true bearing, ward regens after quiet, and holding the Drowned Chapel charges the point. | Reticle, HitMarker, AmmoCounter, DamageIndicator, Killfeed, CompassStrip, SegmentedBar, WorldAnchorLayer, TitleCard, InputPrompt, RadialMenu, TextNode, UINode, QuadNode, NineSliceNode | ?freeze=1, ?assert=1 | READY |
| social-hud.html | The Guildhall (rebuilt whole): guild management, guild chat, and the group — a 34-soul roster you sort, rank, and prune from a real table (right-click legality, confirmed removals), a message of the day you set, chat carrying guild/officer/party voices and /w whispers, and party UNIT frames with a leader crown, a fallen ally, a lost connection, and a ready check that answers on its own clock. | PartyPanel, Table, ChatBox, Select, EldWindow, EldContextMenu, EldPopup, RadialMenu, Button, TextNode, UINode, QuadNode | ?freeze=1, ?assert=1 | READY |
| tycoon.html | The Counting House: a profit line that survives losses, department bars hanging below the zero line, a five-thousand-row finance ledger that expands and sorts without forgetting, and an advisor center whose counsel is prioritized, read, and jumped to. | LineChart, BarChart, TreeTable, NotificationCenter, SpeedControl, ResourceChip, RadialMenu, Window, Button, TextNode, UINode, QuadNode | ?freeze=1, ?assert=1 | READY |
| arcade.html | A real match-three: swaps that only commit when they match, matches that slide and lock before they clear, cascades that multiply, seeded refills, a hint that proves its move — against a moves budget and a running clock, ending in the counting-number tally with its rank seal. | ScoreTally, LevelSelectGrid, StarRating, BigTimer, attachBadge, FloatText, RadialMenu, Button, TextNode, UINode, QuadNode, NineSliceNode | ?freeze=1, ?assert=1 | READY |
| inventory.html | The Reliquary: bags, vendor, loot, currency, the full item context menu on the stacking drag protocol, and a detail pane that reads whatever stack the cursor crosses. | ItemGrid, EldContextMenu, EldPopup, EldDragDrop, NumberField, EventLog, EldToast, RadialMenu | ?freeze=1, ?assert=1 | READY |
| trees.html | Lattices of Fate: three disciplines on one chapter wall — a binder spends insight point by point, tier by tier, toward three separate ruins. | Eldritch, TalentPanes, RadialMenu, EldPopup, EldTooltip, UINode, QuadNode, TextNode, runAudit, hitTest, COLORS, FONTS | ?freeze=1, ?assert=1 | READY |
| settings.html | A real settings screen: tabs, keybinds with conflicts, apply/revert, persistence — opened over a keeper's cabin. | Tabs, Form, KeybindField, Slider, Select, ToggleLabel, RadioLabel, RadialMenu, Button, EldPopup, EldToast, UINode, QuadNode | ?freeze=1, ?assert=1 | READY |
| icon-browser.html | The Icon Vault: the full 3,840-icon pack on a virtualized shelf — search by name/slug/type/tag, filter by category or sheet, walk the shelves from ONE tab stop, click or Enter a tile for its record and a copy-ready snippet, and register your OWN painters beside the shipped ones. Sheets stream in as the grid reaches them and are released behind it; the whole vault costs ZERO atlas entries. | VirtualList, VBox, HBox, TextField, Select, Button, EldToast, EldTooltip, IconButton, IconPalette, iconBase, glow, noGlow, RadialMenu, UINode, QuadNode, TextNode | ?freeze=1, ?assert=1, ?shotdata=1 | READY |
| embed.html | The interface as a good citizen inside a busy host page, with destroy/reboot. | EldWindow, Button, EldTooltip, EventLog, EldEvents, VBox, RadialMenu | ?freeze=1, ?assert=1 | READY |
| index.html | The hub: links and a one-line purpose for every example page. | none | ?assert=1 | READY |
| debug-widgets.html | One of (nearly) everything, booted through the public entry — the quick eyeball page. | Button, IconButton, TextField, ToggleLabel, RadioLabel, Slider, Select, ProgressBar, Tabs, ActionBar, EldToast, EldPopup, EldTooltip, EldWindow | ?freeze=1, ?assert=1 | READY |
| selftest.html | The scripted behavior suite; the title reports its PASS count. | none | none | PASS |
| selftest-lifecycle.html | Boot/destroy/re-boot leak assertions; the title reports its PASS count. | none | none | PASS |
| selftest-geometry.html | The placement-invariant audit over live scenes; the title reports its PASS count. | none | none | PASS |
| selftest-cookbook.html | Checker v4: COOKBOOK snippets tagged `verify` run headless — recipes are tests; the title reports its PASS count. | none | none | PASS |
| debug-core.html | Engine smoke test: the raw renderer booted without the widget suite (internals viewer — deep imports; rAF shimmed for the test suite). | none | none | PASS |
| debug-atlas.html | Every generated texture with the seed scrubber; the title reports the live count (internals viewer — deep imports). | none | none | SKIP |
